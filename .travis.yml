language: node_js

node_js: stable

install:
  - npm install

script:
  - hexo clean
  - hexo generate

after_script:
  - cd ./public
  - git init
  - git config user.name "opsforce"
  - git config user.email "minng.jao@gmail.com"
  - git add .
  - git commit -m "update myblog"
  - git push --force --quiet "https://${github_token}@${GH_REF}" master:master

branches:
  only:
    - gh-pages

env:
  global:
    - GH_REF: github.com/opsforce/myblog.git  